{% set flashes = __app.session.flashes('warning') %}
{% if flashes %}
    <div class="alert warning mb-15">
        {% if flashes|length > 1 %}
            <ul>
                {% for flash in flashes %}
                    <li>{{ flash.message }}</li>
                {% endfor %}
            </ul>
        {% else %}
            <p>{{ flashes[0].message|nl2br }}</p>
        {% endif %}
    </div>
{% endif %}